name: validate JSON
on:
 push:
  - "*.json"
env:
 FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
jobs:
 validate-json:
  runs-on: ubuntu-latest
  steps:
   - name: get checkout
     uses: actions/checkout@v5
   - name: validate JSON
     run: |
      echo "."